/**
 * Danh sách vai trò (role) trong hệ thống — khớp dữ liệu seed bảng `roles`.
 * - ADMIN:   quản trị hệ thống, quản lý kho tri thức/thủ tục.
 * - CITIZEN: công dân sử dụng dịch vụ.
 */
export enum Role {
  ADMIN = 'ADMIN',
  CITIZEN = 'CITIZEN',
}
