# ==================================================
# CIVICAI - Biến môi trường mẫu (TEMPLATE)
# Sao chép thành .env và điền giá trị thật.
# KHÔNG commit file .env vào git.
# ==================================================

# ---- General ----
NODE_ENV=development
APP_NAME=CIVICAI

# ---- Frontend ----
# (bổ sung khi khởi tạo module Frontend)

# ---- Backend / API ----
# Xem chi tiết tại backend/.env.example (PORT, API_PREFIX, LOG_LEVEL...)

# ---- Database (PostgreSQL + pgvector, docker-compose) ----
DB_HOST=localhost
DB_PORT=5433
DB_USER=vaic
DB_PASSWORD=vaic_dev_password
DB_NAME=vaic

# ---- Authentication ----
# (bổ sung khi khởi tạo Authentication)

# ---- AI Service ----
# (bổ sung khi khởi tạo AI Service)

# ---- Storage ----
# (bổ sung khi khởi tạo Storage)

# ---- Gateway ----
# (bổ sung khi khởi tạo Gateway)
