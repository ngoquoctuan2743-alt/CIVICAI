import type { NextConfig } from 'next';

/**
 * Cấu hình Next.js của VAIC 2026 Frontend.
 * PHASE 5 sẽ bổ sung rewrite /api -> backend khi tích hợp.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
